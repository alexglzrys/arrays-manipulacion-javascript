const technologies = ['PHP', 'HTML', 'CSS', 'JavaScript', 'MySQL'];

// El método forEach permite recorrer los elementos de un array sin tener que preocuparse por la dimensión del mismo
technologies.forEach(element => console.log(element));